[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "haussmann"
version = "0.1.0"
description = "Procedural Haussmann building generator"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
